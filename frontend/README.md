# VoxDocs Frontend

This is a Vite + React (JavaScript) frontend for VoxDocs. It uses Tailwind CSS and connects to the backend via `VITE_API_URL`.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Set the backend root URL in `.env`:

```
VITE_API_URL=http://localhost:8000
```
